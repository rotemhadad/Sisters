// HomeScreen.integration.test.js
import React from 'react';
import { render } from '@testing-library/react-native';
import HomeScreen from '../../components/HomeScreen';

// describe('HomeScreen integration tests', () => {

//     // test('navigates to Guest screen when guest button is pressed', () => {
//     //     const navigationMock = {
//     //         navigate: jest.fn(),
//     //     };

//     //     const { getByText } = render(<HomeScreen navigation={navigationMock} />);
//     //     const guestButton = getByText('כניסה בתור משתמש אורח');
//     //     guestButton.props.onPress();

//     //     expect(navigationMock.navigate).toHaveBeenCalledWith('Guest');
//     // });

//     // Add more integration tests as needed
// });
